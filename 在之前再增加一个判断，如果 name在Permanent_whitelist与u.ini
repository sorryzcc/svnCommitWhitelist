在之前再增加一个判断，如果 name在Permanent_whitelist与username一样，则不用再判断name是否在one_time_whitelist.直接返回200

帮我修改上面的代码，我在one_time_whitelist表里新增了一个字段count，永久白名单的逻辑不变，还是如果名单在Permanent_whitelist表里，就返回200.
如果不在Permanent_whitelist表里，在one_time_whitelist表里，如果用户使用hook一次，则当前name的count减少一次，count为0的时候则这个用户在one_time_whitelist表里没有了